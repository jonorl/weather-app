export class todo {
  constructor(id, title, description, dueDate, priority, notes, checkbox) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.dueDate = dueDate;
    this.priority = priority;
    this.notes = notes;
    this.checkbox = checkbox;
  }
}
